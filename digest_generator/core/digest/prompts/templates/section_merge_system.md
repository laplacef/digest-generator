<!--
PLACEHOLDER — generic baseline prompt for the "section merge" stage.

The maintainer's tuned prompt was removed before publishing and backed up
locally. A generic, topic-neutral baseline goes here in Phase 3 of the
source-generalization refactor (and prompt overrides let users ship their own).
See .claude/source-generalization-plan.md.
-->
