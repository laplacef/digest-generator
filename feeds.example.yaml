# Feeds configuration for digest-generator.
#
# `digest-generator init` writes this starter; edit it to define your own
# sections and feeds. The loader looks for feeds.yaml via --feeds, then
# DIGEST_CONFIG, then ./digest-generator/, then ~/.config/digest-generator/.

# Categories are the digest's sections, in order. Each has a stable id and a
# display title. Feeds reference a category by its id. Define your own; the
# ids below are only examples.
categories:
  - id: ai
    title: "AI & Machine Learning"
  - id: engineering
    title: "Engineering"
  - id: security
    title: "Security"

# Each feed needs three fields:
#   name      stable id; becomes the fetch cache filename and each entry's origin
#   url       the RSS/Atom feed URL
#   category  one of the category ids defined above
feeds:
  - name: openai-news
    url: https://openai.com/news/rss.xml
    category: ai

  - name: huggingface-blog
    url: https://huggingface.co/blog/feed.xml
    category: ai

  - name: github-blog
    url: https://github.blog/feed/
    category: engineering

  - name: krebs-on-security
    url: https://krebsonsecurity.com/feed/
    category: security

  - name: the-hacker-news
    url: https://feeds.feedburner.com/TheHackersNews
    category: security
